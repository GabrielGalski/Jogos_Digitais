# Padrão de assets

```text
assets/
├── boosters/{base,opening,preview,sets}
├── cards/
├── characters/player/{idle,run}
├── enemies/{mimic,minotaur,spirit}
├── tiles/{floor,wall}
├── weapons/booster/
└── old/
```
